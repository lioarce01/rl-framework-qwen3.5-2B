# Core training stack
transformers>=4.49.0
datasets>=3.3.0
trl>=0.16.0
peft>=0.14.0
bitsandbytes>=0.45.0
accelerate>=1.3.0

# Utilities
torch>=2.6.0
numpy>=1.26.0
scipy>=1.13.0  # for math evaluation helpers

# Optional — install separately if supported by GPU driver:
# flash-attn>=2.7.0  (pip install flash-attn --no-build-isolation)

# NOTE: Do NOT use unsloth with GRPO — active VRAM leak bug (github.com/unslothai/unsloth/issues/3864)
# If using unsloth for SFT only, pin: unsloth>=2026.3.0
